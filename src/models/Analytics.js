export default class Analystics{
    constructor({prev_day_total, prev_vs_prevday, weekly_avg, monthly_avg}) {
        this.prev_day_total = prev_day_total;
        this.prev_vs_prevday = prev_vs_prevday;
        this.weekly_avg = weekly_avg;
        this.monthly_avg = monthly_avg;
    }
}

